var e=document.getElementById(`publish`),t=e&&e.dataset.api||``,n=`pilot.publish.draft.v1`,r=[`Email`,`Identity`,`Backend`,`Methods`,`Listing`,`Vendor`,`Review`],i=0,a=()=>({name:``,type:`string`,required:!1,in:``,description:``}),o=()=>({args:``,params_as_flags:!1,passthrough:!1}),s=()=>({name:``,http:{verb:`GET`,path:``},cli:o(),latency:``,description:``,params:[a()]}),c=[[`query`,`?name=value, url-encoded. Default for GET.`],[`path`,`fills {name} in the path, url-encoded (REST).`],[`path_raw`,`fills {name} in the path UNescaped — for URL-in-path APIs, e.g. a fetch/proxy that takes GET /<rawurl>.`],[`body`,`JSON body field. Default for POST/PUT/PATCH.`],[`header`,`sent as a request header.`]],l=[`GET`,`POST`,`PUT`,`PATCH`,`DELETE`];function u(e){return e===`GET`||e===`DELETE`?`query`:`body`}function d(e,t){return!!t&&RegExp(`\\{`+t.replace(/[.*+?^${}()|[\]\\]/g,`\\$&`)+`\\}`).test(e||``)}var f=()=>({name:``,value:``}),p=`2026-07-06`,m=h()||{email:``,id:`io.pilot.`,version:``,description:``,app_type:`api`,backend:{base_url:``,headers:[f()],command:``,env_passthrough:``},methods:[s()],listing:{display_name:``,tagline:``,app_description:``,license:``,homepage:``,source_url:``,categories:``,keywords:``,requires_binary:!1,binary_url:``},vendor:{name:``,url:``,agent_usage:``,capabilities:``},release:{signer_name:``,agreed:!1}};m.release||={signer_name:``,agreed:!1},m.app_type??=`api`,m.backend.command??(m.backend.command=``),m.backend.env_passthrough??(m.backend.env_passthrough=``),m.methods.forEach(e=>{e.cli||=o(),e.http||={verb:`GET`,path:``},(e.params||[]).forEach(e=>{e.in??=``})});function h(){try{return JSON.parse(localStorage.getItem(n))}catch{return null}}function g(){localStorage.setItem(n,JSON.stringify(m))}function _(e){return String(e??``).replace(/[&<>"]/g,e=>({"&":`&amp;`,"<":`&lt;`,">":`&gt;`,'"':`&quot;`})[e])}function v(){let e=m.id.lastIndexOf(`.`);return e>=0?m.id.slice(e+1):``}function y(e){return`<span class="info" data-tip="${_(e)}">i</span>`}function b(){let e=e=>(e||``).split(`,`).map(e=>e.trim()).filter(Boolean),t=m.app_type===`cli`,n=t?{type:`cli`,command:e(m.backend.command),env_passthrough:e(m.backend.env_passthrough)}:{type:`http`,base_url:m.backend.base_url,headers:m.backend.headers.filter(e=>e.name.trim())};return{id:m.id,version:m.version,description:m.description,email:m.email,backend:n,methods:m.methods.filter(e=>e.name.trim()).map(n=>{let r=n.params.filter(e=>e.name.trim()).map(e=>{let r={name:e.name,type:e.type,required:!!e.required,description:e.description};return t||(r.in=e.in||u(n.http.verb)),r}),i={name:n.name,description:n.description,latency:n.latency,params:r};return t?i.cli={args:n.cli.passthrough?[]:e(n.cli.args),params_as_flags:!!n.cli.params_as_flags,passthrough:!!n.cli.passthrough}:i.http={verb:n.http.verb,path:n.http.path},i}),listing:{...m.listing,categories:e(m.listing.categories),keywords:e(m.listing.keywords)},vendor:{...m.vendor,contact:m.email},release:{agreement_version:p,signer_name:m.release.signer_name,agreed:!!m.release.agreed}}}function x(){return`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Your email</h2></div>
    <p class="lede2">We'll use this to send you a submission confirmation and the review decision.</p>
    <div class="card">
      <div class="field"><label>Email ${y(`Where we send your submission confirmation and the approval/rejection notice.`)}</label>
        <input id="f-email" type="email" autocomplete="email" placeholder="you@company.com" value="${_(m.email)}">
        <div class="err" id="e-email"></div></div>
    </div>${A()}`}function S(){document.getElementById(`stepper`).innerHTML=r.map((e,t)=>`<li class="${t<i?`done`:t===i?`now`:``}" ${t===i?`aria-current="step"`:``}><span class="dot">${t<i?`✓`:t+1}</span><span class="lbl">${e}</span></li>`).join(``)}var C=document.getElementById(`stepRoot`);function w(e){let t=e.querySelector(`label, .ghead`);if(!t)return``;let n=t.cloneNode(!0);return n.querySelectorAll(`.info`).forEach(e=>e.remove()),(n.textContent||``).replace(/\s+/g,` `).trim()}function T(){C.querySelectorAll(`.field`).forEach((e,t)=>{let n=e.querySelector(`input, select, textarea`),r=e.querySelector(`label`),a=w(e);!n||!a||(r?(n.id||=`publish-field-`+i+`-`+t,r.htmlFor=n.id):n.hasAttribute(`aria-label`)||n.setAttribute(`aria-label`,a))}),Object.entries({"h-name":`Header name`,"h-value":`Header value`,"m-name":`Method name`,"m-verb":`HTTP verb`,"m-path":`HTTP path`,"m-latency":`Latency`,"m-description":`Method description`,"p-name":`Parameter name`,"p-type":`Parameter type`,"p-req":`Parameter required`,"p-in":`Parameter location`,"p-desc":`Parameter description`}).forEach(([e,t])=>{C.querySelectorAll(`.`+e).forEach(e=>{e.hasAttribute(`aria-label`)||e.setAttribute(`aria-label`,t)})}),C.querySelectorAll(`.err`).forEach(e=>e.setAttribute(`aria-live`,`polite`)),C.querySelectorAll(`button[title]`).forEach(e=>{e.hasAttribute(`aria-label`)||e.setAttribute(`aria-label`,e.title)})}function E(e=!1){if(S(),C.innerHTML=k[i](),T(),B(),(r[i]===`Methods`||r[i]===`Review`)&&G(),O(),e){let e=C.querySelector(`.stephdr h2`);e&&(e.tabIndex=-1,e.focus())}}var D=`pilot.publish.ui.v1`;function O(){try{localStorage.setItem(D,JSON.stringify({step:i,entered:!document.getElementById(`form-view`).hidden}))}catch{}}var k=[()=>x(),()=>`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Identity</h2></div>
    <div class="card">
      <div class="row2">
        <div class="field"><label>App ID ${y(`Reverse-DNS id under the io.pilot namespace. Lowercase letters, digits, hyphens.`)}</label>
          <div class="prefixed"><span class="pfx">io.pilot.</span><input id="f-idname" placeholder="weather" value="${_(m.id.replace(/^io\.pilot\./,``))}"></div>
          <p class="desc">Your app installs as <code>${_(m.id||`io.pilot.…`)}</code>.</p><div class="err" id="e-id"></div></div>
        <div class="field"><label>Version ${y(`Semver. Bump for every new build.`)}</label>
          <input id="f-version" placeholder="0.1.0" value="${_(m.version)}"><div class="err" id="e-version"></div></div>
      </div>
      <div class="field"><label>One-line description ${y(`What the app does, shown in the store and to agents.`)}</label>
        <input id="f-description" placeholder="Current weather and forecasts." value="${_(m.description)}"><div class="err" id="e-description"></div></div>
    </div>${A()}`,()=>`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Backend</h2></div>
    <p class="desc" style="margin:-8px 0 16px">What kind of app are you publishing?</p>
    <div class="choices">
      <label class="choice ${m.app_type===`api`?`sel`:``}">
        <input type="radio" name="apptype" value="api" ${m.app_type===`api`?`checked`:``}>
        <div><b>HTTP API <span class="badge ok">available</span></b><span>Your app is reachable over HTTP. We generate, sign, and verify an adapter that forwards each method to it.</span></div>
      </label>
      <label class="choice ${m.app_type===`cli`?`sel`:``}">
        <input type="radio" name="apptype" value="cli" ${m.app_type===`cli`?`checked`:``}>
        <div><b>CLI <span class="badge ok">available</span></b><span>Front a command-line tool installed on the host. Each method runs a subprocess; <code>pilotctl appstore call</code> translates into <code>&lt;cli&gt; &lt;args&gt;</code>.</span></div>
      </label>
    </div>
    ${m.app_type===`cli`?N():M()}
    ${A()}`,()=>`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Methods</h2></div>
    <p class="desc" style="margin:-8px 0 16px">Each method is one call an agent can make. Give it a clear name, the backend route, a latency class, a description, and its parameters. The live preview shows exactly what agents will see and run.</p>
    <div id="methods">${m.methods.map((e,t)=>I(e,t)).join(``)}</div>
    <button class="iconbtn" id="add-method" type="button">+ method</button>
    <div class="err" id="e-methods"></div>
    <div class="preview" id="preview"></div>${A()}`,()=>`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Listing</h2></div>
    <p class="desc" style="margin:-8px 0 16px">How the app appears in the store.</p>
    <div class="card">
      <div class="row2">
        <div class="field"><label>Display name</label><input id="f-l-display_name" placeholder="Weather" value="${_(m.listing.display_name)}"></div>
        <div class="field"><label>License (SPDX) ${y(`e.g. MIT, Apache-2.0, AGPL-3.0-or-later`)}</label><input id="f-l-license" placeholder="Apache-2.0" value="${_(m.listing.license)}"></div>
      </div>
      <div class="field"><label>Tagline</label><input id="f-l-tagline" placeholder="One-line pitch for the store card" value="${_(m.listing.tagline)}"></div>
      <div class="field"><label>App description ${y(`Long-form description (markdown ok), shown on the app store page.`)}</label>
        <textarea id="f-l-app_description" placeholder="What the app does, who it is for, what makes it useful…">${_(m.listing.app_description)}</textarea></div>
      <div class="row2">
        <div class="field"><label>Homepage</label><input id="f-l-homepage" placeholder="https://example.com" value="${_(m.listing.homepage)}"></div>
        <div class="field"><label>Source URL</label><input id="f-l-source_url" placeholder="https://github.com/org/app" value="${_(m.listing.source_url)}"></div>
      </div>
      <div class="row2">
        <div class="field"><label>Categories ${y(`Comma-separated, e.g. search, weather`)}</label><input id="f-l-categories" placeholder="weather, data" value="${_(m.listing.categories)}"></div>
        <div class="field"><label>Keywords</label><input id="f-l-keywords" placeholder="weather, forecast" value="${_(m.listing.keywords)}"></div>
      </div>
    </div>${A()}`,()=>`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Vendor</h2></div>
    <p class="desc" style="margin:-8px 0 16px">About you, and how agents should use this app.</p>
    <div class="card">
      <div class="row2">
        <div class="field"><label>Vendor name</label><input id="f-v-name" placeholder="Acme Inc." value="${_(m.vendor.name)}"></div>
        <div class="field"><label>Vendor URL</label><input id="f-v-url" placeholder="https://acme.example" value="${_(m.vendor.url)}"></div>
      </div>
      <p class="desc" style="margin:-6px 0 18px">We'll reach you at: <b>${_(m.email)}</b>.</p>
      <div class="field"><label>How will autonomous AI agents use this app? ${y(`Include package details, whether methods must be called in a specific order, auth/setup, and anything an agent needs to use it correctly.`)}</label>
        <textarea id="f-v-agent_usage" placeholder="e.g. Call search first to get a result id, then fetch with that id. No human steps required…">${_(m.vendor.agent_usage)}</textarea></div>
      <div class="field"><label>List of all capabilities ${y(`Everything this app can do, in plain language — one per line is fine.`)}</label>
        <textarea id="f-v-capabilities" placeholder="- Search the web&#10;- Fetch page contents&#10;- Summarize a URL">${_(m.vendor.capabilities)}</textarea></div>
    </div>${A()}`,()=>`<div class="stephdr"><span class="k">Step ${i+1} of ${r.length}</span><h2>Review &amp; submit</h2></div>
    <div id="submit-banner"></div>
    <div class="card"><table class="review-tbl" id="review-tbl"></table></div>
    <div class="preview" id="preview"></div>
    <div class="card release">
      <h3 class="release-h">Release agreement</h3>
      <p class="release-p">Before your app can be listed, you sign the <a href="/publisher-agreement" target="_blank" rel="noopener">Publisher Release Agreement</a>. In short: you confirm you have the right to publish this app, you grant Pilot Protocol a license to use your name and the app's name to list and promote it in the app store and on our website, and you release us from claims arising from that authorized use. Please read the full agreement before signing.</p>
      <div class="field"><label>Full legal name (your signature) ${y(`Typing your name and submitting signs the Publisher Release Agreement electronically — it carries the same legal effect as a handwritten signature.`)}</label>
        <input id="f-signer" autocomplete="name" placeholder="Jane Doe" value="${_(m.release.signer_name)}"><div class="err" id="e-signer"></div></div>
      <label class="agree"><input type="checkbox" id="f-agree" ${m.release.agreed?`checked`:``}><span>I have read and agree to the <a href="/publisher-agreement" target="_blank" rel="noopener">Publisher Release Agreement</a>, and I confirm I am authorized to publish this app and to grant the rights it describes.</span></label>
      <div class="err" id="e-agree"></div>
    </div>
    <div class="navrow"><button class="back" id="back" type="button">← Back</button><button class="btn" id="submit" type="button" disabled>Submit for review</button></div>`];function A(){return`<div class="navrow">${i>0?`<button class="back" id="back" type="button">← Back</button>`:`<span></span>`}<button class="btn" id="next" type="button">${i===r.length-2?`Review →`:`Next →`}</button></div>`}function j(e,t){return`<div class="prow" data-hi="${t}" style="grid-template-columns:1fr 1.4fr auto">
    <input class="h-name" placeholder="x-api-key" value="${_(e.name)}">
    <input class="h-value" placeholder="\${WEATHER_TOKEN}" value="${_(e.value)}">
    <button class="iconbtn danger h-del" type="button">✕</button></div>`}function M(){return`<div class="explain">Your adapter forwards each method to your app. Add any auth headers it needs.
      A value like <code>\${WEATHER_TOKEN}</code> is a <b>secret placeholder</b>: the operator who installs the app
      supplies it at install time (from their environment or a local secrets file) — it is <b>never</b> stored in the published app.</div>
    <div class="card">
      <div class="field"><label>Base URL ${y(`The production endpoint. Baked in as the default; operators can override.`)}</label>
        <input id="f-baseurl" placeholder="https://api.example.com" value="${_(m.backend.base_url)}"><div class="err" id="e-baseurl"></div></div>
      <div class="field"><label>Auth headers <span class="muted">(optional)</span></label>
        <div id="headers">${m.backend.headers.map((e,t)=>j(e,t)).join(``)}</div>
        <button class="iconbtn" id="add-header" type="button">+ header</button>
      </div>
    </div>`}function N(){return`<div class="explain">We generate, sign, and verify an adapter that runs your command. The command must already be
      installed on the operator's host. The child runs with a <b>scrubbed environment</b> — only the variables you list below
      (plus PATH/HOME/locale) are passed through. The app ships with a <code>proc.exec</code> grant scoped to exactly this command, and installs <b>guarded</b> via the reviewed catalogue.</div>
    <div class="card">
      <div class="field"><label>Command ${y(`The base argv the adapter runs. Comma-separated, e.g. gh  —or—  python, -m, tool. Each method appends its own args.`)}</label>
        <input id="f-command" placeholder="gh" value="${_(m.backend.command)}"><div class="err" id="e-command"></div></div>
      <div class="field"><label>Env passthrough <span class="muted">(optional)</span> ${y(`Host environment variables the CLI may see, comma-separated. Everything else is scrubbed from the child.`)}</label>
        <input id="f-envpass" placeholder="GH_TOKEN, AWS_PROFILE" value="${_(m.backend.env_passthrough)}"></div>
    </div>`}function P(e){return`<div class="field"><span class="ghead">Verb</span><select class="m-verb">${l.map(t=>`<option ${e.http.verb===t?`selected`:``}>${t}</option>`).join(``)}</select></div>
      <div class="field"><span class="ghead">Path ${y(`Backend route, e.g. /search or /items/{id}. Use {name} placeholders to insert a param into the path (set that param's "in" to path or path_raw below). By default GET/DELETE params become the query string and POST/PUT/PATCH params become the JSON body.`)}</span><input class="m-path" placeholder="/search  or  /items/{id}" value="${_(e.http.path)}"></div>`}function F(e){let t=!!e.cli.passthrough,n=`<span class="ghead" style="margin-top:12px">Arguments ${y("Comma-separated argv appended to the command. Use ${param} to insert a payload field, e.g. current, --lat, ${lat}")}</span>
      <input class="m-args" placeholder="current, --lat, \${lat}" value="${_(e.cli.args)}">
      <label class="toggle" style="margin-top:8px"><input type="checkbox" class="m-flags" ${e.cli.params_as_flags?`checked`:``}> <span>Also append each parameter as <code>--name value</code></span></label>`;return`<div class="field" style="margin-top:4px">
      <label class="toggle"><input type="checkbox" class="m-passthrough" ${t?`checked`:``}> <span>Passthrough — front the whole CLI (translate any <code>&lt;cli&gt; &lt;args&gt;</code>)</span></label>
      ${t?`<p class="desc" style="margin:6px 0 0">Every subcommand is reachable; the caller supplies the argv. Call it as <code>{"args":["…"]}</code> — no baked arguments.</p>`:n}
    </div>`}function I(e,t){let n=m.app_type===`cli`,r=n?` style="grid-template-columns:2fr 1fr"`:``,i=n?``:P(e);return`<div class="mcard" data-mi="${t}">
    <div class="mhead"><span class="mname">${_(e.name||v()+`.method`)}</span>${m.methods.length>1?`<button class="iconbtn danger m-del" type="button">Remove</button>`:``}</div>
    <div class="mgrid"${r}>
      <div class="field"><span class="ghead">Method name ${y(`Prefixed with your namespace, e.g. `+_(v()||`app`)+`.search`)}</span><input class="m-name" placeholder="${_(v()||`app`)}.search" value="${_(e.name)}"></div>
      ${i}
      <div class="field"><span class="ghead">Latency ${y(`How long a typical call takes — agents use it to pick the cheapest method that fits. Fast: under 5s · Medium: up to 15s · Slow: up to 1 min.`)}</span><select class="m-latency"><option value="">Select…</option>${[`fast`,`med`,`slow`].map(t=>`<option value="${t}" ${e.latency===t?`selected`:``}>${t===`med`?`Medium`:t[0].toUpperCase()+t.slice(1)}</option>`).join(``)}</select></div>
    </div>
    ${n?F(e):``}
    <div class="field"><span class="ghead">Description ${y(`Shown in the help doc agents read. Be specific about what it returns.`)}</span>
      <textarea class="m-description" placeholder="Search the corpus; returns ranked results with url, title, score.">${_(e.description)}</textarea></div>
    <div class="params${n?``:` http`}">
      <div class="phead"><span class="ghead" style="margin:0">Parameters</span><button class="iconbtn p-add" type="button">+ parameter</button></div>
      <div class="ptable">
        <div class="prow phdr"><span>Name</span><span>Type</span><span>Req</span>${n?``:`<span>In ${y(`Where the adapter reads this param from when it calls your backend. query/path/path_raw fill the URL, body is a JSON field, header is a request header. Defaults to the natural location for the verb (GET → query, POST → body).`)}</span>`}<span>Description</span><span></span></div>
        ${e.params.map((t,n)=>L(t,n,e)).join(``)}
      </div>
    </div>
  </div>`}function L(e,t,n){if(m.app_type===`cli`)return`<div class="prow" data-pi="${t}">
    <input class="p-name" placeholder="q" value="${_(e.name)}">
    <select class="p-type">${[`string`,`int`,`number`,`bool`,`object`].map(t=>`<option ${e.type===t?`selected`:``}>${t}</option>`).join(``)}</select>
    <label class="reqcell"><input type="checkbox" class="p-req" ${e.required?`checked`:``}></label>
    <input class="p-desc" placeholder="what this parameter is" value="${_(e.description)}">
    <button class="iconbtn danger p-del" type="button" title="Remove">✕</button></div>`;let r=e.in||u(n.http.verb),i=c.map(([t,n])=>{let i=t+(!e.in&&t===r?` (default)`:``);return`<option value="${t}" ${e.in===t?`selected`:``} title="${_(n)}">${i}</option>`}).join(``),a=(r===`path`||r===`path_raw`)&&!d(n.http.path,e.name)?`<div class="phint">Add <code>{${_(e.name||`name`)}}</code> to the path to place this param there.</div>`:``;return`<div class="prow" data-pi="${t}">
    <input class="p-name" placeholder="q" value="${_(e.name)}">
    <select class="p-type">${[`string`,`int`,`number`,`bool`,`object`].map(t=>`<option ${e.type===t?`selected`:``}>${t}</option>`).join(``)}</select>
    <label class="reqcell"><input type="checkbox" class="p-req" ${e.required?`checked`:``}></label>
    <div class="p-incell"><select class="p-in">${i}</select>${a}</div>
    <input class="p-desc" placeholder="what this parameter is" value="${_(e.description)}">
    <button class="iconbtn danger p-del" type="button" title="Remove">✕</button></div>`}function R(e,t){let n=document.getElementById(e);n&&n.addEventListener(`input`,()=>{t(n.value),g()})}function z(){var e=document.getElementById(`form-view`);window.scrollTo({top:e?e.offsetTop-12:0,behavior:`smooth`})}function B(){let e=r[i],t=document.getElementById(`back`);t&&(t.onclick=()=>{i=Math.max(0,i-1),E(!0),z()});let n=document.getElementById(`next`);if(n&&(n.onclick=()=>{H()&&(i++,E(!0),z())}),e===`Email`){let e=document.getElementById(`f-email`);e&&e.addEventListener(`input`,()=>{m.email=e.value.trim(),g()})}if(e===`Identity`){let e=document.getElementById(`f-idname`);e&&e.addEventListener(`input`,()=>{m.id=`io.pilot.`+e.value.trim(),document.querySelector(`#publish .prefixed + .desc`)?.replaceChildren(),g();let t=e.closest(`.field`).querySelector(`.desc`);t&&(t.innerHTML=`Your app installs as <code>`+_(m.id)+`</code>.`)}),R(`f-version`,e=>m.version=e),R(`f-description`,e=>m.description=e)}if(e===`Backend`&&(document.querySelectorAll(`input[name="apptype"]`).forEach(e=>{e.addEventListener(`change`,()=>{e.checked&&m.app_type!==e.value&&(m.app_type=e.value,g(),E(),W())})}),m.app_type===`cli`?(R(`f-command`,e=>m.backend.command=e),R(`f-envpass`,e=>m.backend.env_passthrough=e)):(R(`f-baseurl`,e=>m.backend.base_url=e),document.getElementById(`add-header`).onclick=()=>{m.backend.headers.push(f()),g(),E()},document.querySelectorAll(`#headers .prow`).forEach(e=>{let t=+e.dataset.hi;e.querySelector(`.h-name`).addEventListener(`input`,e=>{m.backend.headers[t].name=e.target.value,g()}),e.querySelector(`.h-value`).addEventListener(`input`,e=>{m.backend.headers[t].value=e.target.value,g()}),e.querySelector(`.h-del`).onclick=()=>{m.backend.headers.splice(t,1),m.backend.headers.length||m.backend.headers.push(f()),g(),E()}}))),e===`Methods`&&(document.getElementById(`add-method`).onclick=()=>{m.methods.push(s()),g(),E()},document.querySelectorAll(`#methods .mcard`).forEach(e=>{let t=+e.dataset.mi,n=m.methods[t],r=(t,n)=>{let r=e.querySelector(t);r&&r.addEventListener(`input`,()=>{n(r),g(),W()})};r(`.m-name`,t=>{n.name=t.value,e.querySelector(`.mname`).textContent=t.value||v()+`.method`});let i=e.querySelector(`.m-verb`);i&&i.addEventListener(`change`,()=>{n.http.verb=i.value,g(),E(),W()});let o=e.querySelector(`.m-path`);o&&o.addEventListener(`input`,()=>{n.http.path=o.value,g(),W(),e.querySelectorAll(`.params .prow[data-pi]`).forEach(e=>{let t=+e.dataset.pi,r=n.params[t],i=e.querySelector(`.p-incell`);if(!i)return;let a=r.in||u(n.http.verb),o=i.querySelector(`.phint`);if(o&&o.remove(),(a===`path`||a===`path_raw`)&&!d(n.http.path,r.name)){let e=document.createElement(`div`);e.className=`phint`,e.innerHTML=`Add <code>{`+_(r.name||`name`)+`}</code> to the path to place this param there.`,i.appendChild(e)}})}),r(`.m-args`,e=>n.cli.args=e.value);let c=e.querySelector(`.m-flags`);c&&c.addEventListener(`change`,()=>{n.cli.params_as_flags=c.checked,g(),W()});let l=e.querySelector(`.m-passthrough`);l&&l.addEventListener(`change`,()=>{n.cli.passthrough=l.checked,g(),E(),W()}),r(`.m-latency`,e=>n.latency=e.value),r(`.m-description`,e=>n.description=e.value);let f=e.querySelector(`.m-del`);f&&(f.onclick=()=>{m.methods.splice(t,1),m.methods.length||m.methods.push(s()),g(),E()}),e.querySelector(`.p-add`).onclick=()=>{n.params.push(a()),g(),E()},e.querySelectorAll(`.params .prow[data-pi]`).forEach(e=>{let t=+e.dataset.pi,r=n.params[t],i=()=>{let t=e.querySelector(`.p-incell`);if(!t)return;let i=r.in||u(n.http.verb),a=t.querySelector(`.phint`);if(a&&a.remove(),(i===`path`||i===`path_raw`)&&!d(n.http.path,r.name)){let e=document.createElement(`div`);e.className=`phint`,e.innerHTML=`Add <code>{`+_(r.name||`name`)+`}</code> to the path to place this param there.`,t.appendChild(e)}};e.querySelector(`.p-name`).addEventListener(`input`,e=>{r.name=e.target.value,g(),i(),W()}),e.querySelector(`.p-type`).addEventListener(`input`,e=>{r.type=e.target.value,g(),W()}),e.querySelector(`.p-req`).addEventListener(`change`,e=>{r.required=e.target.checked,g()});let o=e.querySelector(`.p-in`);o&&o.addEventListener(`change`,e=>{r.in=e.target.value,g(),i(),W()}),e.querySelector(`.p-desc`).addEventListener(`input`,e=>{r.description=e.target.value,g()}),e.querySelector(`.p-del`).onclick=()=>{n.params.splice(t,1),n.params.length||n.params.push(a()),g(),E()}})})),e===`Listing`&&[`display_name`,`tagline`,`app_description`,`license`,`homepage`,`source_url`,`categories`,`keywords`].forEach(e=>R(`f-l-`+e,t=>m.listing[e]=t)),e===`Vendor`&&[`name`,`url`,`agent_usage`,`capabilities`].forEach(e=>R(`f-v-`+e,t=>m.vendor[e]=t)),e===`Review`){K();let e=document.getElementById(`f-signer`),t=document.getElementById(`f-agree`),n=document.getElementById(`submit`),r=()=>{n&&(n.disabled=!(m.release.signer_name.trim()&&m.release.agreed))};e&&e.addEventListener(`input`,()=>{m.release.signer_name=e.value,g(),V(`e-signer`,``),K(),r()}),t&&t.addEventListener(`change`,()=>{m.release.agreed=t.checked,g(),V(`e-agree`,``),r()}),r(),n&&(n.onclick=q)}}function V(e,t){let n=document.getElementById(e);n&&(n.textContent=t||``);let r=document.getElementById({"e-id":`f-idname`,"e-methods":`add-method`}[e]||`f-`+e.replace(/^e-/,``));return r&&(r.setAttribute(`aria-describedby`,e),r.setAttribute(`aria-invalid`,t?`true`:`false`)),r}function H(){let e=!0,t=null,n=(n,r)=>{let i=V(n,r);!t&&i&&(t=i),e=!1},a=r[i];if(a===`Email`&&(/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(m.email||``)?V(`e-email`,``):n(`e-email`,`Enter a valid email`)),a===`Identity`&&(/^io\.pilot\.[a-z0-9]([a-z0-9-]*[a-z0-9])?$/.test(m.id)?V(`e-id`,``):n(`e-id`,`Must be io.pilot.<name> (lowercase, digits, hyphens)`),/^\d+\.\d+\.\d+(-[0-9A-Za-z.]+)?$/.test(m.version)?V(`e-version`,``):n(`e-version`,`Semver, e.g. 0.1.0`),m.description.trim()?V(`e-description`,``):n(`e-description`,`Required`)),a===`Backend`&&(m.app_type===`cli`?m.backend.command.trim()?V(`e-command`,``):n(`e-command`,`Enter the command to run, e.g. gh`):/^https?:\/\/[^\s/]+/.test(m.backend.base_url)?V(`e-baseurl`,``):n(`e-baseurl`,`Absolute http(s) URL`)),a===`Methods`){let e=m.methods.filter(e=>e.name.trim());if(!e.length)n(`e-methods`,`Add at least one method`);else if(m.app_type===`cli`)e.find(e=>!e.latency||!e.description.trim()||!e.name.startsWith(v()+`.`)||!e.cli.passthrough&&!e.cli.args.trim()&&!e.cli.params_as_flags)?n(`e-methods`,`Each CLI method needs a `+v()+`.-prefixed name, a latency, a description, and either arguments, params-as-flags, or passthrough`):V(`e-methods`,``);else if(e.find(e=>!e.latency||!e.description.trim()||!/^\//.test(e.http.path)||!e.name.startsWith(v()+`.`)))n(`e-methods`,`Each method needs a `+v()+`.-prefixed name, path starting with /, a latency, and a description`);else{let t=null;e.forEach(e=>e.params.filter(e=>e.name.trim()).forEach(n=>{let r=n.in||u(e.http.verb);(r===`path`||r===`path_raw`)&&!d(e.http.path,n.name)&&!t&&(t={m:e,p:n})})),t?n(`e-methods`,`Param "`+t.p.name+`" is mapped to `+t.p.in+` but {`+t.p.name+`} is not in `+t.m.name+`’s path. Add {`+t.p.name+`} to the path, or change its In.`):V(`e-methods`,``)}}return!e&&t&&(t.focus(),t.scrollIntoView({block:`center`})),e}var U=null;function W(){clearTimeout(U),U=setTimeout(G,400)}async function G(){let e=document.getElementById(`preview`);if(e)try{let n=await(await fetch(t+`/api/preview`,{method:`POST`,headers:{"Content-Type":`application/json`},body:JSON.stringify(b())})).json(),r=(n.help.methods||[]).map(e=>`  <span class="pk">${_(e.method)}</span>  <span class="muted">[${_(e.duration)}]</span>  ${_(e.summary||``)}`).join(`
`),i=(n.commands||[]).map(e=>_(e)).join(`
`);e.innerHTML=`<h4>${_(v()||`app`)}.help (what agents discover)</h4><div class="cmd">${r||`<span class="muted">add a method…</span>`}</div><h4 style="margin-top:16px">pilotctl commands</h4><div class="cmd">${i}</div>`}catch(t){e.innerHTML=`<span class="muted">preview unavailable (${_(t.message)})</span>`}}function K(){let e=b(),t=m.app_type===`cli`,n=e=>t?e.cli.passthrough?`passthrough`:((e.cli.args||[]).join(` `)||`flags`)+(e.cli.params_as_flags?` +flags`:``):`${e.http.verb} ${e.http.path}`,r=e=>(e.params||[]).length?t?(e.params||[]).map(e=>e.name).join(`, `):(e.params||[]).map(e=>`${e.name}→${e.in}`).join(`, `):``,i=[[`App ID`,_(e.id)],[`Version`,_(e.version)],[`Description`,_(e.description)],[`App type`,t?`CLI`:`HTTP API`],t?[`Command`,_((e.backend.command||[]).join(` `))]:[`Backend`,_(e.backend.base_url)],...t?(e.backend.env_passthrough||[]).length?[[`Env passthrough`,_((e.backend.env_passthrough||[]).join(`, `))]]:[]:e.backend.headers.map(e=>[`Header`,_(e.name+`: `+e.value)]),[`Methods`,e.methods.map(e=>{let t=r(e);return _(`${e.name} (${n(e)}, ${e.latency})`)+(t?` <span class="muted">— ${_(t)}</span>`:``)}).join(`<br>`)],[`Display name`,_(e.listing.display_name)],[`License`,_(e.listing.license)],[`App description`,_(e.listing.app_description)],[`Categories`,_((e.listing.categories||[]).join(`, `))],[`Vendor`,_(e.vendor.name+(e.vendor.url?` · `+e.vendor.url:``))],[`Email`,_(e.email)],[`Agent usage`,_(e.vendor.agent_usage)],[`Capabilities`,_(e.vendor.capabilities)],[`Signed by`,_(e.release.signer_name)]];document.getElementById(`review-tbl`).innerHTML=i.map(([e,t])=>`<tr><th>${_(e)}</th><td>${t||`<span style="color:var(--ink-faint)">—</span>`}</td></tr>`).join(``)}async function q(){let e=document.getElementById(`submit`),r=document.getElementById(`submit-banner`);if(!m.release.signer_name.trim()){V(`e-signer`,`Type your full legal name to sign the release agreement`);return}if(!m.release.agreed){V(`e-agree`,`You must agree to the Publisher Release Agreement to submit`);return}let i=b();i.release.signed_at=new Date().toISOString(),e.disabled=!0,r.innerHTML=`<div class="banner">Building, signing, and verifying on the server…</div>`;try{let a=await fetch(t+`/api/submit`,{method:`POST`,headers:{"Content-Type":`application/json`},body:JSON.stringify(i)}),o=await a.json();a.ok?(r.innerHTML=`<div class="banner ok"><b>Submitted ✓</b> — case <code>${_(o.case_id)}</code> is now <b>pending</b> review. The server built, signed, and verified your adapter.</div>`,localStorage.removeItem(n)):(r.innerHTML=`<div class="banner bad"><b>Fix these:</b><ul>${(o.errors||[o.error||`submission failed`]).map(e=>`<li>${_(e)}</li>`).join(``)}</ul></div>`,e.disabled=!1)}catch(t){r.innerHTML=`<div class="banner bad">Could not reach the API: ${_(t.message)}</div>`,e.disabled=!1}}var J=document.getElementById(`start`),Y=document.getElementById(`exit`),X=document.getElementById(`intro`),Z=document.getElementById(`form-view`);J&&(J.onclick=function(){X.hidden=!0,Z.hidden=!1,O(),window.scrollTo(0,0)}),Y&&(Y.onclick=function(){Z.hidden=!0,X.hidden=!1,O(),window.scrollTo(0,0)});try{var Q=JSON.parse(localStorage.getItem(D)||`{}`);Q&&Q.entered&&(i=Math.min(Q.step||0,r.length-1),X.hidden=!0,Z.hidden=!1)}catch{}E();